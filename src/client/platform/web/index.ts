import { UNTITLED } from '../../../constant/STRING'
import render from './render'

render({
  name: UNTITLED,
  units: {
    root: {
      path: '9aba266d-5200-4281-b477-749c9b3c5815',
    },
  },
  component: {
    slots: [['root', 'default']],
    subComponents: {
      root: {},
    },
    children: ['root'],
  },
})
